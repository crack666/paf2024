package de.vfh.paf.tasklist.infrastructure.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration class for the Task List application.
 * Beans are now defined with @Service and @Repository annotations on the respective classes.
 */
@Configuration
public class TaskListConfig {
    // All beans are now auto-configured via annotations
    // This class is kept for backward compatibility
}